{% extends 'base.html.twig' %}

{% block title %}Edit Cases{% endblock %}

{% block body %}
    <div class="container">
    <h1>Edit Cases</h1>

    {{ include('cases/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_cases_index') }}">back to list</a>
    {{ include('cases/_delete_form.html.twig') }}
    </div>
{% endblock %}
