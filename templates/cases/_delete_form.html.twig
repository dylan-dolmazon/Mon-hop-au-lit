<form method="post" action="{{ path('app_cases_delete', {'id': case.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ case.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
