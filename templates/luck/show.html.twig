{% extends 'base.html.twig' %}

{% block title %}Luck{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="row justify-content-center">Luck</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ luck.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ luck.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_luck_index') }}">back to list</a>

    <a href="{{ path('app_luck_edit', {'id': luck.id}) }}">edit</a>

    {{ include('luck/_delete_form.html.twig') }}
    </div>
{% endblock %}
